const COMMAND_PREFIX = 'vscode-sailpoint-nerm';

export const VIEW_CONTAINER_ID = `${COMMAND_PREFIX}.view`;
export const DROP_MIME_TYPE = 'application/vnd.code.tree.vscode-sailpoint-nerm.view';

export const NEW_FOLDER_ROOT = `${COMMAND_PREFIX}.folder.add-root`;
export const NEW_FOLDER = `${COMMAND_PREFIX}.folder.add`;
